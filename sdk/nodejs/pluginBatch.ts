// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * <!-- markdownlint-disable MD033 -->
 *
 * This resource allows you to enable or disable multiple RabbitMQ plugins in a single batch
 * operation. Compared to [cloudamqp.Plugin], this resource manages a set of plugins together and
 * minimises the number of API calls by sending only what has changed on update.
 *
 * Only available for dedicated subscription plans running ***RabbitMQ***.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudamqp from "@pulumi/cloudamqp";
 *
 * const instance = new cloudamqp.Instance("instance", {
 *     name: "terraform-cloudamqp-instance",
 *     plan: "bunny-1",
 *     region: "amazon-web-services::us-east-1",
 *     tags: ["terraform"],
 * });
 * const plugins = new cloudamqp.PluginBatch("plugins", {
 *     instanceId: instance.id.apply(x =>Number(x)),
 *     plugins: {
 *         rabbitmq_stomp: true,
 *         rabbitmq_top: true,
 *         rabbitmq_web_mqtt: true,
 *         rabbitmq_random_exchange: false,
 *     },
 * });
 * ```
 *
 * <details>
 *   <summary>
 *     <b>
 *       <i>Faster instance destroy when running `terraform destroy`</i>
 *     </b>
 *   </summary>
 *
 * Set `enableFasterInstanceDestroy` to ***true*** in the provider configuration to skip disabling
 * plugins when destroying the instance.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudamqp from "@pulumi/cloudamqp";
 *
 * const instance = new cloudamqp.Instance("instance", {
 *     name: "terraform-cloudamqp-instance",
 *     plan: "bunny-1",
 *     region: "amazon-web-services::us-east-1",
 *     tags: ["terraform"],
 * });
 * const plugins = new cloudamqp.PluginBatch("plugins", {
 *     instanceId: instance.id.apply(x =>Number(x)),
 *     plugins: {
 *         rabbitmq_stomp: true,
 *         rabbitmq_top: true,
 *         rabbitmq_web_mqtt: true,
 *     },
 * });
 * ```
 *
 * </details>
 *
 * ## Behaviour
 *
 * ### Create
 *
 * Sends an enable request containing all plugins in the map with value `true`.
 *
 * ### Update
 *
 * Computes the minimal diff between the previous and desired state:
 *
 * * Plugins changed to `true` (or newly added as `true`) are sent in the **enable** list.
 * * Plugins changed to `false` (or removed from the map, treated as disabled) are sent in the
 *   **disable** list.
 *
 * Only the changed plugins are sent to the API, keeping the request as small as possible.
 *
 * ### Delete
 *
 * Sends a disable request for all plugins in the map with value `true`. Plugins already set to
 * `false` are ignored.
 *
 * ## Dependency
 *
 * This resource depends on the CloudAMQP instance identifier, `cloudamqp_instance.instance.id`.
 *
 * ## Enable faster instance destroy
 *
 * When running `terraform destroy` this resource will try to disable the managed plugins before
 * deleting `cloudamqp.Instance`. This is not necessary since the servers will be deleted.
 *
 * Set `enableFasterInstanceDestroy` to ***true*** in the provider configuration to skip this.
 *
 * ## Required plugins
 *
 * The following plugins are always enabled by CloudAMQP and do not need to be managed:
 *
 * | Name                      | Version |
 * |---------------------------|---------|
 * | rabbitmqManagement       | all     |
 * | rabbitmqManagementAgent | all     |
 * | rabbitmqPrometheus       | 3.10.0  |
 * | rabbitmqWebDispatch     | all     |
 *
 * [cloudamqp.Plugin]: plugin.md
 *
 * ## Import
 *
 * Not possible to import this resource.
 */
export class PluginBatch extends pulumi.CustomResource {
    /**
     * Get an existing PluginBatch resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: PluginBatchState, opts?: pulumi.CustomResourceOptions): PluginBatch {
        return new PluginBatch(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'cloudamqp:index/pluginBatch:PluginBatch';

    /**
     * Returns true if the given object is an instance of PluginBatch.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is PluginBatch {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === PluginBatch.__pulumiType;
    }

    /**
     * The CloudAMQP instance ID.
     */
    declare public readonly instanceId: pulumi.Output<number>;
    /**
     * A map of plugin name to enabled state. Set a plugin to `true` to
     * enable it, or `false` to disable it. Only the plugins listed in this map are
     * managed by this resource; all other plugins on the instance are left untouched.
     */
    declare public readonly plugins: pulumi.Output<{[key: string]: boolean}>;
    /**
     * Configurable sleep time (seconds) used when polling for job completion.
     * Default set to 10 seconds.
     */
    declare public readonly sleep: pulumi.Output<number>;
    /**
     * Configurable timeout time (seconds) for the operation. Default set to
     * 1800 seconds.
     */
    declare public readonly timeout: pulumi.Output<number>;

    /**
     * Create a PluginBatch resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: PluginBatchArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: PluginBatchArgs | PluginBatchState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as PluginBatchState | undefined;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["plugins"] = state?.plugins;
            resourceInputs["sleep"] = state?.sleep;
            resourceInputs["timeout"] = state?.timeout;
        } else {
            const args = argsOrState as PluginBatchArgs | undefined;
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            if (args?.plugins === undefined && !opts.urn) {
                throw new Error("Missing required property 'plugins'");
            }
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["plugins"] = args?.plugins;
            resourceInputs["sleep"] = args?.sleep;
            resourceInputs["timeout"] = args?.timeout;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(PluginBatch.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering PluginBatch resources.
 */
export interface PluginBatchState {
    /**
     * The CloudAMQP instance ID.
     */
    instanceId?: pulumi.Input<number | undefined>;
    /**
     * A map of plugin name to enabled state. Set a plugin to `true` to
     * enable it, or `false` to disable it. Only the plugins listed in this map are
     * managed by this resource; all other plugins on the instance are left untouched.
     */
    plugins?: pulumi.Input<{[key: string]: pulumi.Input<boolean>} | undefined>;
    /**
     * Configurable sleep time (seconds) used when polling for job completion.
     * Default set to 10 seconds.
     */
    sleep?: pulumi.Input<number | undefined>;
    /**
     * Configurable timeout time (seconds) for the operation. Default set to
     * 1800 seconds.
     */
    timeout?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a PluginBatch resource.
 */
export interface PluginBatchArgs {
    /**
     * The CloudAMQP instance ID.
     */
    instanceId: pulumi.Input<number>;
    /**
     * A map of plugin name to enabled state. Set a plugin to `true` to
     * enable it, or `false` to disable it. Only the plugins listed in this map are
     * managed by this resource; all other plugins on the instance are left untouched.
     */
    plugins: pulumi.Input<{[key: string]: pulumi.Input<boolean>}>;
    /**
     * Configurable sleep time (seconds) used when polling for job completion.
     * Default set to 10 seconds.
     */
    sleep?: pulumi.Input<number | undefined>;
    /**
     * Configurable timeout time (seconds) for the operation. Default set to
     * 1800 seconds.
     */
    timeout?: pulumi.Input<number | undefined>;
}
