// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.cloudamqp;

import com.pulumi.cloudamqp.PluginBatchArgs;
import com.pulumi.cloudamqp.Utilities;
import com.pulumi.cloudamqp.inputs.PluginBatchState;
import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.String;
import java.util.Map;
import javax.annotation.Nullable;

/**
 * &lt;!-- markdownlint-disable MD033 --&gt;
 * 
 * This resource allows you to enable or disable multiple RabbitMQ plugins in a single batch
 * operation. Compared to [cloudamqp.Plugin], this resource manages a set of plugins together and
 * minimises the number of API calls by sending only what has changed on update.
 * 
 * Only available for dedicated subscription plans running ***RabbitMQ***.
 * 
 * ## Example Usage
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.cloudamqp.Instance;
 * import com.pulumi.cloudamqp.InstanceArgs;
 * import com.pulumi.cloudamqp.PluginBatch;
 * import com.pulumi.cloudamqp.PluginBatchArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var instance = new Instance("instance", InstanceArgs.builder()
 *             .name("terraform-cloudamqp-instance")
 *             .plan("bunny-1")
 *             .region("amazon-web-services::us-east-1")
 *             .tags("terraform")
 *             .build());
 * 
 *         var plugins = new PluginBatch("plugins", PluginBatchArgs.builder()
 *             .instanceId(instance.id())
 *             .plugins(Map.ofEntries(
 *                 Map.entry("rabbitmq_stomp", true),
 *                 Map.entry("rabbitmq_top", true),
 *                 Map.entry("rabbitmq_web_mqtt", true),
 *                 Map.entry("rabbitmq_random_exchange", false)
 *             ))
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * &lt;details&gt;
 *   &lt;summary&gt;
 *     &lt;b&gt;
 *       &lt;i&gt;Faster instance destroy when running `terraform destroy`&lt;/i&gt;
 *     &lt;/b&gt;
 *   &lt;/summary&gt;
 * 
 * Set `enableFasterInstanceDestroy` to ***true*** in the provider configuration to skip disabling
 * plugins when destroying the instance.
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.cloudamqp.Instance;
 * import com.pulumi.cloudamqp.InstanceArgs;
 * import com.pulumi.cloudamqp.PluginBatch;
 * import com.pulumi.cloudamqp.PluginBatchArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var instance = new Instance("instance", InstanceArgs.builder()
 *             .name("terraform-cloudamqp-instance")
 *             .plan("bunny-1")
 *             .region("amazon-web-services::us-east-1")
 *             .tags("terraform")
 *             .build());
 * 
 *         var plugins = new PluginBatch("plugins", PluginBatchArgs.builder()
 *             .instanceId(instance.id())
 *             .plugins(Map.ofEntries(
 *                 Map.entry("rabbitmq_stomp", true),
 *                 Map.entry("rabbitmq_top", true),
 *                 Map.entry("rabbitmq_web_mqtt", true)
 *             ))
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * &lt;/details&gt;
 * 
 * ## Behaviour
 * 
 * ### Create
 * 
 * Sends an enable request containing all plugins in the map with value `true`.
 * 
 * ### Update
 * 
 * Computes the minimal diff between the previous and desired state:
 * 
 * * Plugins changed to `true` (or newly added as `true`) are sent in the **enable** list.
 * * Plugins changed to `false` (or removed from the map, treated as disabled) are sent in the
 *   **disable** list.
 * 
 * Only the changed plugins are sent to the API, keeping the request as small as possible.
 * 
 * ### Delete
 * 
 * Sends a disable request for all plugins in the map with value `true`. Plugins already set to
 * `false` are ignored.
 * 
 * ## Dependency
 * 
 * This resource depends on the CloudAMQP instance identifier, `cloudamqp_instance.instance.id`.
 * 
 * ## Enable faster instance destroy
 * 
 * When running `terraform destroy` this resource will try to disable the managed plugins before
 * deleting `cloudamqp.Instance`. This is not necessary since the servers will be deleted.
 * 
 * Set `enableFasterInstanceDestroy` to ***true*** in the provider configuration to skip this.
 * 
 * ## Required plugins
 * 
 * The following plugins are always enabled by CloudAMQP and do not need to be managed:
 * 
 * | Name                      | Version |
 * |---------------------------|---------|
 * | rabbitmqManagement       | all     |
 * | rabbitmqManagementAgent | all     |
 * | rabbitmqPrometheus       | 3.10.0  |
 * | rabbitmqWebDispatch     | all     |
 * 
 * [cloudamqp.Plugin]: plugin.md
 * 
 * ## Import
 * 
 * Not possible to import this resource.
 * 
 */
@ResourceType(type="cloudamqp:index/pluginBatch:PluginBatch")
public class PluginBatch extends com.pulumi.resources.CustomResource {
    /**
     * The CloudAMQP instance ID.
     * 
     */
    @Export(name="instanceId", refs={Integer.class}, tree="[0]")
    private Output<Integer> instanceId;

    /**
     * @return The CloudAMQP instance ID.
     * 
     */
    public Output<Integer> instanceId() {
        return this.instanceId;
    }
    /**
     * A map of plugin name to enabled state. Set a plugin to `true` to
     * enable it, or `false` to disable it. Only the plugins listed in this map are
     * managed by this resource; all other plugins on the instance are left untouched.
     * 
     */
    @Export(name="plugins", refs={Map.class,String.class,Boolean.class}, tree="[0,1,2]")
    private Output<Map<String,Boolean>> plugins;

    /**
     * @return A map of plugin name to enabled state. Set a plugin to `true` to
     * enable it, or `false` to disable it. Only the plugins listed in this map are
     * managed by this resource; all other plugins on the instance are left untouched.
     * 
     */
    public Output<Map<String,Boolean>> plugins() {
        return this.plugins;
    }
    /**
     * Configurable sleep time (seconds) used when polling for job completion.
     * Default set to 10 seconds.
     * 
     */
    @Export(name="sleep", refs={Integer.class}, tree="[0]")
    private Output<Integer> sleep;

    /**
     * @return Configurable sleep time (seconds) used when polling for job completion.
     * Default set to 10 seconds.
     * 
     */
    public Output<Integer> sleep() {
        return this.sleep;
    }
    /**
     * Configurable timeout time (seconds) for the operation. Default set to
     * 1800 seconds.
     * 
     */
    @Export(name="timeout", refs={Integer.class}, tree="[0]")
    private Output<Integer> timeout;

    /**
     * @return Configurable timeout time (seconds) for the operation. Default set to
     * 1800 seconds.
     * 
     */
    public Output<Integer> timeout() {
        return this.timeout;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public PluginBatch(java.lang.String name) {
        this(name, PluginBatchArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public PluginBatch(java.lang.String name, PluginBatchArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public PluginBatch(java.lang.String name, PluginBatchArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("cloudamqp:index/pluginBatch:PluginBatch", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private PluginBatch(java.lang.String name, Output<java.lang.String> id, @Nullable PluginBatchState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("cloudamqp:index/pluginBatch:PluginBatch", name, state, makeResourceOptions(options, id), false);
    }

    private static PluginBatchArgs makeArgs(PluginBatchArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? PluginBatchArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static PluginBatch get(java.lang.String name, Output<java.lang.String> id, @Nullable PluginBatchState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new PluginBatch(name, id, state, options);
    }
}
